The files in this directory contain applications or extensions of ranger-async which
are put here for your inspiration and as references.

In order to use a plugin from this directory, you need to copy it to
~/.config/ranger-async/plugins/

Note that if you update ranger-async to a new minor version (for example,
from 1.6.* to 1.7.0), your outdated plugins WILL break and crash ranger-async.
